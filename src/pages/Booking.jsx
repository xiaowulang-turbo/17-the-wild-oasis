import BookingDetail from "../features/bookings/BookingDetail";

// Do not write side effects in the pages components, only in the feature components
export default function Booking() {
  return <BookingDetail />;
}
